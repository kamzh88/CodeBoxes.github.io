import React, { Component } from "react";

class SearchBooks extends Component {
    render() {
        return (

            <div>Search Books Div</div>

        );
    };
};

export default SearchBooks;